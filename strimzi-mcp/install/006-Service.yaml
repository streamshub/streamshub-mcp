apiVersion: v1
kind: Service
metadata:
  name: streamshub-strimzi-mcp
  namespace: streamshub-mcp
  labels:
    app: streamshub-strimzi-mcp
spec:
  selector:
    app: streamshub-strimzi-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  type: ClusterIP
