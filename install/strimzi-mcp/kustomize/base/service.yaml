apiVersion: v1
kind: Service
metadata:
  name: streamshub-mcp-strimzi
spec:
  selector:
    app.kubernetes.io/name: streamshub-mcp-strimzi
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  type: ClusterIP
