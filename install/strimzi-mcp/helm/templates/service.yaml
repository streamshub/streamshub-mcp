apiVersion: v1
kind: Service
metadata:
  name: {{ include "strimzi-mcp.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "strimzi-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "strimzi-mcp.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
